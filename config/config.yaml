defaults:
  - retrieval: default
  - evaluation: default
  - pipeline: default
  - augmentation: default
  - _self_

name: autorag-live
version: "0.1.0"

paths:
  data_dir: ./data
  cache_dir: ./.cache
  runs_dir: ./runs

logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

cache:
  embeddings:
    enabled: true
    max_size: 10000
    ttl: 3600  # 1 hour
  retrieval:
    enabled: true
    max_size: 1000
    ttl: 1800  # 30 minutes
